#include "lists.h"

/**
 * add_nodeint_end - adds a new node at the end of a doubly linked list
 * @head: double pointer to the list
 * @n: data to insert in the new node
 * Return: the address of the new element, or NULL if if failed
 */
dlistint_t *add_dnodeint_end - adds a new node at the end of a doubly linked list
@head: double          
